@model IEnumerable<RedBadge.Models.IndividualResultsModels.IRListItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Results Records</h1>

    @Html.ActionLink("Add Results", "Post", "IndividualResults")

<div>
<div>
<ul>

@{
    String OccasionCache = "";
}

@foreach (var item in Model) {
    <text>
    @if (item.OccasionName != OccasionCache)
    {
        OccasionCache = item.OccasionName;
        @Html.Raw("</ul></div></div>")

        <text>
            <div class="card mb-4">
                <div class="card-body">
        <h5 class="card-title">@Html.DisplayFor(modelItem => item.OccasionName)</h5>
        <h6 class="card-subtitle mb-2 text-muted">@Html.DisplayFor(modelItem => item.GameName)</h6>
        <ul class="list-group list-group-flush">

        </text>
    }
    </text>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                        @Html.DisplayFor(modelItem => item.PlayerName)
                    <span class="badge badge-pill badge-secondary">@Html.DisplayFor(modelItem => item.RankName)</span>
                    </li>
}